/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental.serverComponentsExternalPackages was renamed in Next 14;
  // child_process is a built-in Node module and needs no special config.
};

module.exports = nextConfig;
